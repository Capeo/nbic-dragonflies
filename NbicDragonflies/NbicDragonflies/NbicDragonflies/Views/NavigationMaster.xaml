<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:utility="clr-namespace:NbicDragonflies.Utility;assembly=NbicDragonflies"
             x:Class="NbicDragonflies.Views.NavigationMaster"
             Padding="40,0,0,0"
             Title="Navigation"
             Icon="hamburger.png">
  <ContentPage.Content>
    
    <StackLayout VerticalOptions="FillAndExpand">
	  <StackLayout x:Name="LogoField">
        <Image Source="artsdatabanken.png" WidthRequest="180" HeightRequest="120" Margin="0,40,0,0"/>
      </StackLayout>
      <ListView x:Name="NavigationList" VerticalOptions="FillAndExpand" SeparatorVisibility="None" RowHeight="45">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
			  <StackLayout x:Name="NavigationStack" Orientation="Horizontal" Padding="10" Spacing="10" Margin="10,0,0,0">
				<Image Source="{Binding IconSource}"/>
				<Label Font="20" Text="{Binding Title}"/>
			  </StackLayout>
			</ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>