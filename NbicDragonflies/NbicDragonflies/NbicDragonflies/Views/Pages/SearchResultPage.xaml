<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:utility="clr-namespace:NbicDragonflies.Utility;assembly=NbicDragonflies"
             x:Class="NbicDragonflies.Views.Pages.SearchResultPage"
             Title="Søk">
  <ContentPage.Content>
    <StackLayout x:Name="SearchPageLayout" BackgroundColor="{x:Static utility:Constants.Background}">
      <SearchBar x:Name="SpeciesSearchBar" Placeholder="Søk arter..." BackgroundColor="{x:Static utility:Constants.Background}" Margin="0" HorizontalOptions="FillAndExpand"/>
      <ScrollView x:Name="HomeScrollView" Orientation="Vertical" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <ScrollView.Content>
          <StackLayout x:Name="SearchStackLayout" Spacing="5" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <Label x:Name="LabelHeader" TextColor="Gray" HorizontalOptions="FillAndExpand" FontSize="18" Margin="10,0,0,0"/>
          </StackLayout>
        </ScrollView.Content>
      </ScrollView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>
